[tool.poetry]
name = "snake"
version = "0.1.0"
description = ""
authors = ["ctrlVadim <vaidm.haliullin02@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pyyaml = "^6.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
